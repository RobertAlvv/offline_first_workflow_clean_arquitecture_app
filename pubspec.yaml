name: offline_first_workflow
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.0-374.0.dev <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  dartz: ^0.10.1
  equatable: ^2.0.5
  flag: ^6.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  flutter_dotenv: ^5.0.2
  flutter_masked_text2: ^0.9.1
  get_it: ^7.2.0
  google_fonts: ^3.0.1
  hive_flutter: ^1.1.0
  intl: ^0.18.0
  local_datasource:
    path: packages/data/local_datasource
  lottie: ^2.1.0
  network_info:
    path: packages/data/network_info
  remote_datasource:
    path: packages/data/remote_datasource
  utils_material:
    path: packages/ui/utils_material

dev_dependencies:
  hive_generator: ^2.0.0
  build_runner: ^2.3.3
  flutter_lints: ^2.0.0
  flutter_native_splash: ^2.2.16
  flutter_test:
    sdk: flutter
  very_good_analysis: ^3.1.0

flutter:

  uses-material-design: true

  assets:
    - .env
    - assets/


flutter_native_splash:
  color: "#000000"
  image: assets/icon_app.png
  android: true